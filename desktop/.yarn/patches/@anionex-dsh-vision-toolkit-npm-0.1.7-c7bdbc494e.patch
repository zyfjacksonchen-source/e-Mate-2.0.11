diff --git a/lib/tools.js b/lib/tools.js
index 4cac143b7b109c0830ba5ed79c24dd37cfae5042..d6bda44e75fa8df55ba6a859138d24e70c925110 100644
--- a/lib/tools.js
+++ b/lib/tools.js
@@ -153,11 +153,13 @@ export function createVisionTools(source, projectPresentation = presentationIden
                 render: renderJson,
             },
             async execute(args, exec) {
+                const query = args.query?.trim();
+                const region = args.region?.trim();
                 const request = {
                     images: args.images,
-                    ...(args.query === undefined ? {} : { query: args.query }),
+                    ...(query ? { query } : {}),
                     ...(args.ocr === true ? { ocr: true } : {}),
-                    ...(args.region === undefined ? {} : { region: args.region }),
+                    ...(region ? { region } : {}),
                 };
                 return runtimeFrom(source).glance(request, callOptions(exec, args.timeoutMs, lifecycleSignal));
             },
diff --git a/src/tools.ts b/src/tools.ts
index 587093220cd4f0fe8168d48b26947c13cfd4cb48..da691c9552601a6ee7f242d430208da013171b6d 100644
--- a/src/tools.ts
+++ b/src/tools.ts
@@ -195,11 +195,13 @@ export function createVisionTools(
         render: renderJson,
       },
       async execute(args: GlanceArgs, exec) {
+        const query = args.query?.trim()
+        const region = args.region?.trim()
         const request: GlanceRequest = {
           images: args.images,
-          ...(args.query === undefined ? {} : { query: args.query }),
+          ...(query ? { query } : {}),
           ...(args.ocr === true ? { ocr: true } : {}),
-          ...(args.region === undefined ? {} : { region: args.region }),
+          ...(region ? { region } : {}),
         }
         return runtimeFrom(source).glance(request, callOptions(exec, args.timeoutMs, lifecycleSignal))
       },
