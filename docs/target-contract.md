# e-Mate 2.0.15 target contract

This file is the implementation source of truth. Development records may add evidence but may not weaken these obligations.

## 2.0.15 execution overlay

- The user-authorized next-version target is `2.0.15`, integrated on `release/2.0.15` from the clean current canonical `origin/main` source `5f8c54db7b76276c14f1938c970df155f4e6fd80`. The exact source/public-state split, ticket gates and evidence are recorded in [`docs/2.0.15/BASELINE.md`](2.0.15/BASELINE.md) and [`docs/2.0.15/STATUS.md`](2.0.15/STATUS.md).
- T18 source freeze fixes the 19 managed package identities at `2.0.15` and binds the successor Base contract below. This is source truth only: no 2.0.15 candidate, installer, installed receipt or release exists yet. Public Desktop pointers and all three desired states still identify the accepted `2.0.13` production bytes; the 2.0.14 source and candidate receipts remain historical inputs and never became a formal release.
- The 2.0.14 single-hotfix restrictions below remain the historical contract for 2.0.14. They do not prohibit the separately authorized 2.0.15 scope, but every existing native-owner, trust, compatibility, data, updater, publication and exact-byte invariant remains binding. No 2.0.15 ticket may create a second Session, Workspace, Tool registry, Skill store, Scheduler, Job bus, Share transcript, message model, Desktop shell or updater.
- T00 is governance-only. Product implementation is partitioned across T01–T17 by the exact path leases in [`docs/2.0.15/DECISIONS.md`](2.0.15/DECISIONS.md). T18 may freeze version/Base/source after all T01–T17 product/component work is merged and the final Harness is advertised-reachable; formal candidate, installed predecessor/update/rollback and production publication remain separately gated and OPEN until their exact receipts exist.

## Identity and source pins

- Product name: `e-Mate`
- Repository: `zyfjacksonchen-source/e-Mate-2.0.11`
- Desktop Base source freeze: `@e-mate/desktop@2.0.15`
- Product Profile source freeze: `2.0.15`
- Accepted predecessor: the formally published e-Mate `2.0.13` Base v7, three-target Profile desired state, website and installed receipts frozen by [`slices/2.0.14.md`](slices/2.0.14.md). Its product source is `6f63b5238d5874a1973a5ce0c1c9f832d277c865`; the accepted docs-only closeout is `4474939fe186739a7745584c6589e55695302cb2`. Neither identity may be relabelled as this release source.
- Historical Git ancestry floor: accepted e-Mate 2.0.11 source `6a7f4b9d59a1d8970345638946fb6564e2f5f93e`. Every 2.0.15 release candidate must still descend from it; this source-integrity floor does not replace the installed 2.0.13 regression baseline above.
- Source-frozen successor Base contract: `e-mate-desktop-profile-v8-dsh-4787caf39134`. Immutable v7 remains the accepted 2.0.13 predecessor and is not silently changed. Every retained component accepts only v8, binds Harness `4787caf39134df190105b272da0dd2ba893d4d75`, and declares the exact emitted runtime ABI it consumes. The v8 `runtime_imports` keys equal the 15 retained components' declaration/emission union, exclude the unconsumed `@deepseek-ai/dsh-launch-environment`, and bind `@e-mate/desktop/vision-toolkit` to `2.0.15`. This source identity is not releasable until candidate, complete Profile, installed-byte and rollback receipts agree.
- The published 2.0.12 Base reads only `desktop/latest.json`. Its frozen 2.0.13 value is exactly `2961` bytes / SHA-256 `d26b9ffb5f30531bc5de6c9f66aab47c3718248e2ff109d82cd3a763f0c02887`, but that target Helper cannot consume the predecessor's request. Because 2.0.14 was never formally released, 2.0.15 is the one corrective bridge: after exact public 2.0.12→candidate and 2.0.13→candidate installation/rollback acceptance, publication writes immutable objects and the manual manifest, CAS-activates and publicly rereads `desktop/signed/latest.json`, then CAS-advances `desktop/latest.json` from only that frozen 2.0.13 identity to the exact same admitted 2.0.15 manifest bytes last. The legacy pointer then freezes at 2.0.15. A local/test build, different manifest, unsigned nearby artifact, arbitrary same-version predecessor, skipped public-predecessor test or non-conditional overwrite is forbidden.
- Executable: `e-mate`
- Harness source: `zyfjacksonchen-source/deepseek-harness@4787caf39134df190105b272da0dd2ba893d4d75`, advertised by `refs/heads/release/e-mate-2.0.15-harness` (upstream `deepseek-ai/deepseek-harness@99f6f02fecdb7dff40c3fbc9470f5907c29f74ca` plus the accepted bounded Session, Schedule, Job, Tool, model-directory, prompt, redundant-permission, attachment-overlay, registration-bound `llm/wire`, native Composer-frame and stable Settings-section changes recorded below)
- Harness source version: `0.1.0-rc.7`
- Desktop reference: `anywhere-labs/deepseek-harness-desktop@6074088f5b660206e404b3591fab51fb99c69add`; its `upstream.json`, root resolutions, Desktop dependencies and lock all establish the same DSH `0.1.0-rc.7` ABI. e-Mate derives platform lifecycle behavior from that reference while keeping the accepted Harness fork above.
- Browser shell and component source: task `019ff91c-47ca-7c11-93bd-863475181a18`, represented by the final e-Mate 2.0.4/2.0.5 UI state at exact 2.0.5 commit `564a6b6c1d43fb6831dd4a5cd8026e472f063311`
- Chat interaction source: Codex prototype `019ff665-d721-79a0-869d-338f086cf529`
- “Harness” identifies the pinned technical foundation only and must not be used as the product or UI name.

## 2.0.13 overlay invariants

The older version-specific sections below remain binding where they describe preserved behavior. The following 2.0.13 invariants take precedence where a historical version number or acceptance baseline differs:

- Ordinary model calls retain the frozen 2.0.12 system header, Tool header, route, selected model, reasoning level, ApiProxy carrier, policy, quota and audit semantics byte-for-byte. Pet, canvas, previews, Goal presentation and shared details are browser/UI projections and do not enter the ordinary resident Tool header.
- Performance and TTFT v2 are optional diagnostics, not Desktop/Profile admission, signing, coordinator, publication, website, or rollback prerequisites. Their tooling remains default-off and may be dispatched independently against exact installed bytes; a missing, failed, superseded, fixture, or incomplete diagnostic is reported only as such and cannot block or satisfy release admission.
- The admitted Desktop manifest is schema v2 and contains no `performance` field. GitHub artifact provenance contains only the exact `desktop_candidate`; release-state schema v3 contains CI, Profile, Desktop, admission and publication stages only. No consumer may invent a waiver, sentinel, boolean pass, `performance_run_id`, receipt, summary, or signature to fill the removed field.
- The one current Goal is owned exclusively by pinned rc.7 `@deepseek-ai/dsh-goal`: durable state is the `goal/change` stream and `goal` Session Projection, mutation is CAS `{id, revision}`, live continuation authority is process-local, and restart/fork disarms without rewriting durable phase. `@目标`, natural-language creation, Agent creation, Queue/Todo/Schedule associations and the expanded Goal surface must reuse the native Command/Tool/Remote/Projection/round-driver path; none may create a second Goal record, driver or scheduler.
- A Base that can write Schedule delivery protocol v2 cannot run that delivery drive while a first-launch update is still on probation. Renderer visibility is not the commit boundary: the native updater acknowledgement must first durably commit the exact candidate, and an acknowledgement failure must leave no v2 write before restoring a v1 Base. After that commit, every Base/update manifest carries an explicit Schedule protocol floor and the updater rejects any target below the highest committed floor unless the target is explicitly forward-compatible with every persisted event. A live Session-fold admission check may additionally guard a planned downgrade, but cannot replace the probation latch or protocol floor, invent a second Schedule store, or make crash rollback depend on a live Cordis service.
- One shared right-side details area is the only product container for Goal details, project files, canvas and typed deliverable previews. It owns layout/navigation state only; each capability continues to read and mutate its native owner. Opening a panel cannot copy Goal, Queue, Schedule, Attachment, Artifact or file state into a browser store.
- Better Sidebar remains the project-file owner already admitted by the Profile. 2.0.13 makes its Explorer/file service reachable through “项目文件” and the shared details area, while not exposing or cloning its Terminal, Git, Subagent or Browser workbench pages in the e-Mate product surface.
- Cowart is an interaction reference only. The shipped canvas is a first-party Profile component over pinned MIT Excalidraw with all fonts/assets self-hosted. It persists project pages through the existing Host file boundary, accepts materials by Attachment/Artifact identity, sandboxes HTML without same-origin authority, and adds no cloud collaboration, telemetry, model route, credential or store.
- Multi-image work remains N independent native `imagegen` Tool/Job executions, orchestrated by the pinned rc.7 parent/Subagent path: the parent delegates exactly one requested output or one selected-source edit to each bounded child, and only aggregates successful child Job/Attachment identities. The native Job/Queue owner remains the dispatch authority; no child owns a batch, scheduler, queue or retry loop. Default active image concurrency is 1 across children; the release may raise it only after the measured 1→2→4 step-up contract in [`performance-and-acceptance.md`](performance-and-acceptance.md), never above 4. A failed or unknown invocation is not retried automatically while the upstream lacks an idempotency/status query.
- Every rc.7 native Subagent Session remains durable and reachable only through its parent task's native Subagent/catalog address, Job/Attachment receipts and diagnostic/export paths; it is never a top-level e-Mate product task. The Shell derives this one navigation classification from the native `SessionListState` using durable `origin: 'subagent'`, lineage `parentId`, `subagentsByParent` child entries and `currentAddress`, then applies it identically to project/general lists, search, batch deletion and Home counts/Token/trends/recent tasks. Partial, out-of-order, reconnected, restarted and hot-activated projections must never flash a child into those surfaces. Selecting a child keeps the parent row highlighted without rewriting current Session, deleting/archiving the child, hiding the native Subagent entry or filtering by title/prompt text.
- An authenticated, policy-ready ImageGen capability is not healthy unless the exact current Agent scope can discover and execute the one native `imagegen` Tool. Profile activation, new/existing Session creation, Tool Search restriction and client remount must reconcile capability health against the actual scoped Tool identity; a catalog card, component process or server route alone cannot claim readiness. If a requested Office/deliverable workflow requires generated images and the Tool is unavailable or an image Job fails, the terminal result must explicitly remain partial/blocked and must not describe placeholders or specifications as completed image deliverables.
- An image edit is successful only when a real edit Tool/Job receipt names the exact source Attachment(s), a distinct verified output Attachment is committed, and a post-edit check proves the requested invariant on the output. For exact text replacement, the old text must be absent and the required replacement present at every requested region within the accepted OCR/region-confidence contract; otherwise the item remains failed/partial and may be retried explicitly. Model narration, an output filename, a copied source image, a changed SHA alone or a claimed region count is not completion evidence.
- Typed terminal deliverables are projected only from real successful Tool/Job events and their Attachment/Artifact identities. Markdown/DOM scanning, filename guessing, browser-manufactured success and a Goal completion card that cites a missing or failed artifact are prohibited.
- Online task sharing retains the one `/emate.share` Host adapter, Session ZIP exporter, authenticated share service and dedicated share bucket. Host and client must consume one shared versioned result schema; every terminal create/list/revoke outcome is either the schema's success branch or a bounded typed failure branch. A validation-library diagnostic, raw upstream body or unrecognized discriminator must never become user-visible error text. Fix the producer or shared contract once rather than adding a browser-only catch-all, retry transport or second sharing path.
- Enterprise user identity remains unique at the Auth database and normalized-account boundary. The management client renders an idempotent projection keyed by the server's immutable user identity and replaces, rather than concatenates, a requested page/snapshot. A visual `Set`, name-based merge or hidden duplicate is not a repair: 2.0.13 must distinguish server-row duplication from pagination/retry/race duplication, fix the first owner that violates uniqueness, and leave an auditable data-quality receipt for any historical-row repair.
- Task scenario is decided only by the local `emate.audit` producer from the pinned rc.7 task/Session event stream and a closed allowlist of first-party Tool or bundled Skill identities. One turn is one task and locks one existing `TaskScenario` value for its whole lifecycle; an explicit bundled Skill signal wins, otherwise the first recognized first-party Tool signal wins, and insufficient or conflicting evidence falls back to `GENERAL`. The enterprise plane renders this ledger value verbatim and must not infer from titles or content. Prompt/output text, Tool arguments/results, private Tool/Skill names, paths and file contents never enter the task-audit payload; historical `GENERAL` facts remain immutable. This classification remains the 2.0.14 target: the user-scoped 2.0.13 P0/TQ incident release keeps the predecessor's `GENERAL` facts and must not ship the unaccepted classifier. The already-enforced single write path (`emate.audit` to Model Gateway task ledger) remains a security invariant across both versions; Analytics/Admin must not regain a caller-supplied task-event write endpoint or credential.
- Attaching a project folder uses the one pinned Desktop native directory-picker capability and then the native WorkspaceRegistry attach path. A composed browser picker that serves only `browse` must never be selected as an implementation of `host.pickDirectory`; capability composition, feature detection and failure reporting are fixed at their shared owner rather than hidden behind a second picker, typed path field or browser filesystem API.
- A newly created conversation becomes visible only from the native Session create/route/persistence projection and remains addressable across list refresh, route changes and restart. The Shell must not add an optimistic browser-only row, drop a valid empty Session because title/message projection has not arrived, or create a second home/session fallback; a create acknowledgement that cannot be reconciled is an explicit pending/error state rather than a disappearing task.
- Route, Session and product-surface identity are one atomic Shell projection. `/schedules`, `/capabilities`, home/new-session pending state and `/chat/<sessionId>` must never combine the body, header, composer, model, project scope or actions from different routes or Session revisions. A standalone route shows only its own chrome; Home resolves the target through rc.7 `connectWorkspace()`, synchronously opens the returned native blank Session, then commits `/` without ever clearing the old Session first. Pending/rejected resolution preserves the prior usable surface, and a result whose source route or Session changed is discarded. A newly acknowledged native Session may show a bounded pending surface until its baseline arrives but cannot reuse the old Session body/composer; an old Session renders only after route id and authoritative Session id agree. Back/forward, rapid clicks and out-of-order Session/Workspace baselines must discard stale projection work rather than partially commit it. Returning to an owning Session restores the same native Session surface from the route projection, with no duplicated browser route or Session state.
- Registration preserves the shared identity contract: unique account, required real name, matching password of 10–256 characters and one unexpired server-issued image challenge. The form and server consume one validation vocabulary; a password shorter than 10 characters shows an inline error at the password field, while confirmation and challenge errors remain attached to their own fields. Valid input submits exactly once to the existing identity endpoint and projects its pending-approval receipt. A permanently disabled button with no reason, client/server rule drift, CAPTCHA bypass, weakened password floor, duplicated submission path or browser-manufactured success is forbidden.
- Explicit logout follows the one `emate.identity` lease-revocation path and a bounded typed result. The enterprise endpoint treats an already expired/revoked lease idempotently, never emits an unhandled 500 for a valid logout request, and increments/reconciles the existing auth revision as contracted. The device must stop using and remove its local/keystore-backed lease after the explicit logout action even when remote completion is unknown; the UI may distinguish local logout from pending/failed remote revocation but must not retain an effective local login, expose raw transport text, claim remote success without a receipt, or create a second logout/retry transport.
- A Session's selected model remains owned by pinned rc.7 Session/Agent configuration and is reconciled against the current authenticated, agreement-unlocked managed catalog. Authentication or agreement loss may make the model temporarily unusable but must not erase its durable Session identity or silently replace it; after the native identity/catalog baselines recover, the same allowed model reappears, while a genuinely revoked model shows one typed unavailable/reselection state. Browser storage, a hard-coded default, policy bypass and a second model-selection owner are forbidden.
- The e-Mate Shell's admitted composer slot is the sole ordinary product presentation of the native rc.7 input lifecycle. Profile activation, identity/catalog refresh, Session/Workspace baseline races and client remounts must preserve the same semantic composer boundary and slot winner. The unbranded upstream DSH composer may remain available only as the pinned core implementation/developer fallback, not flash or become the ordinary user surface; if the e-Mate Shell component is unhealthy, the product shows an explicit bounded component-health/recovery state instead of silently changing composer ownership, duplicating the input tree or hiding one implementation with CSS.
- The desktop pet reuses only the MIT code from `npc-dao/dsh-pet@f501139cfb155fd46717a79bb1c158da064dce15`; its upstream `dsh`/`aliang` sprite assets are license-excluded and cannot be copied. e-Mate is default-on as the independently licensed 112px 小芯 v2 sprite, maps structured Goal/Todo/Queue/Job/Tool/Deliverable state into the contracted 30 office scenes, pauses during first response/background/minimized/reduced-motion states, and never consumes token deltas or internal reasoning.
- CDP stays the first browser path. Computer Use can run only after the latest direct user message carries the exact `@电脑操控` authority; the two paths hand off serially at native Tool boundaries and never control the same page concurrently. AutoGPTBrowser and any second browser Agent/runtime are outside this release.
- UI release notes describe user capabilities only. Package, plugin, upstream-project and internal component names must not be shown to ordinary users.
- Capability Center interactive cards and its collapsed-sidebar entry expose the same visible hover and keyboard-focus boundary in light/dark themes. Hover styling is CSS/theme-token presentation only, cannot change readiness/actions or add pointer listeners, and reduced-motion removes movement without removing the focus boundary.
- A confirmed macOS Base update has one canonical installed owner: the exact accepted candidate must replace `/Applications/e-Mate.app` atomically, restart from that resolved bundle, and become the bytes returned by the next ordinary `open` of the canonical path. Quarantine/trust handling belongs to the existing native updater and platform signing/install boundary after manifest/signature/hash verification; the product must not print or execute a generic `xattr -dr` workaround, launch a cached/translocated copy, leave the old bundle at the canonical path while claiming success, or clear attributes outside the exact verified candidate. A failed install/open/health check restores the exact prior bundle and receipt.
- The final installed 2.0.12 artifact/Profile combination remains the regression baseline. When optional TTFT diagnostics run, they use that same baseline; missing timing evidence does not block release.

## First development principle: native Creation Mode before permanent plugins

- “对照 DSH 原生” always means the Harness and Desktop-reference pins declared by the current checked-in `desktop/e-mate-desktop/base-contract.json`, never a floating upstream branch, latest package, another RC, or an unrelated Desktop release. Before implementation, the development record names the exact e-Mate Base id, Harness version/commit, and Desktop-reference commit used as evidence; component locks, runtime imports and composition tests must resolve to those same pins or fail closed.
- The pinned rc.7 `cordis` preset (“创造模式”) is the first development path for every feature that can be expressed as a DSH Host or Client plugin. Development starts by loading the shipped plugin/composition authoring skill, querying the live runtime with the native `cordis_inspect_*` providers, and exercising the smallest reversible dynamic package through the native define/run/stop/undefine lifecycle. Runtime Services, Events, Tools, Slots, themes, and lifecycle reports are authoritative; screenshots and guessed interfaces are not.
- A dynamic Cordis package is only a process-local prototype. It is not a Profile component, does not survive restart, cannot be promoted by copying memory state, and cannot bypass repository admission. The accepted result must be reimplemented as an ordinary immutable Profile plugin and pass exact Base/Harness compatibility, component and composition tests, signature and provenance validation, atomic generation activation, renderer-health commit, and rollback.
- Desktop preserves the complete shipped system-preset root from the pinned DSH package, including `standard`, `code`, `minimal`, and `cordis`, as the reference Desktop does. e-Mate may shadow only `standard` from a separate managed system root to supply the product persona. The ordinary product UI hides preset selectors and the preset-management page, but it must not edit, remove, or disable the shipped preset tree or make Creation Mode the default; `cordis` remains available to the native Agent/development path required by this contract.
- Creation Mode has shell-equivalent trust and is explicit opt-in. Dynamic code receives no signing secret, component publication credential, desired-state activation authority, or automatic production deployment path. Electron lifecycle, native helpers, platform packaging/signing, updater trust, and any other Base-only behavior remain on the pinned `deepseek-harness-desktop` path; inability to model them dynamically is recorded rather than concealed behind a fake plugin.
- Repository gates must fail if the shipped `cordis` preset and its two authoring skills are absent from the packaged runtime, if Desktop composition replaces the shipped preset root with a single custom preset, or if a dynamic-package directory/source becomes a release payload. The development log records the inspected seam, experiment outcome, permanent component mapping, and narrow acceptance command for each applicable slice.

## Product boundaries

- Local Harness owns execution, sessions, tools, jobs, plugins, memory, schedules, and artifacts.
- Native Tool Mode uses the first-party `@e-mate/dsh-plugin-tool-search` Profile component for progressive Tool schema disclosure. It installs only a per-Agent `ToolRuntime.restrict()` view plus `tool_search`: a small explicit allowlist remains visible, matching original inherited Tools are revealed on the next model step, Code Mode keeps its generated SDK path, and browser Tools remain visible so CDP stays the first web path. Agent-local Tools remain directly visible exactly as rc.7 requires; in particular `@deepseek-ai/dsh-schedule` owns `schedule_create`, `schedule_list`, and `schedule_delete` in the live Agent scope and Tool Search must neither mask nor proxy them. Selection recovery reads only the pinned Agent Loop's existing `request/header.tools`; the component must not proxy execution, create a second Tool registry/dispatcher, or append a downstream Session event.
- Scheduled-task management is the first-party hot Profile component `@e-mate/dsh-plugin-schedules`. It may expose only a loopback, read-only cross-session projection over persisted native Schedule events: active records retain rc.7 `scheduled`/`overdue`, one-shot dispatches may be shown as completed, and the native dispatch stream may be shown as recent runs. The standalone `/schedules` page creates, edits, and deletes by returning the user to the owning DSH session and using the native Agent-local `schedule_create`, `schedule_list`, and `schedule_delete` Tools; execution, recurrence, timers, state, recovery, and events remain exclusively owned by pinned rc.7 `@deepseek-ai/dsh-schedule`. rc.7 defines no pause, resume, edit, run-now or separate run-history Tool/event, so the UI must not claim those actions; edit is an explicit create-success-then-delete handoff in the owning session. A second scheduler, task table, timer, dispatcher, history store or direct UI mutation endpoint is a contract failure.
- The enterprise administrator/control plane owns exactly three classes of behavior: identity/account/agreement/authentication leases; managed model and search route policy with bounded credential leases; and append-only redacted audit/usage ingestion and projection. A model request may transport the local caller's Tool schemas and return the model's Tool-call selection as inference output, but only the local Harness may dispatch or execute it.
- The enterprise management UI may invoke only a manually initiated bounded connectivity probe through the existing authenticated same-origin Model Gateway session: catalog lookup plus one minimal text Responses request or capability-selected image generation. It must not receive upstream keys, accept arbitrary prompts, attach Tool schemas, call local Harness, persist generated content, or create a second provider/transport; quota enforcement and redacted usage audit remain mandatory.
- The enterprise administrator/control plane may not read, mutate, authorize, enable, disable, install, dispatch, execute, publish, or retain local Tools, plugins/Skills, files, workspaces, sessions, memory, artifacts, Computer Use/CDP, schedules/jobs, share content, Profile/updater/release state, or external-connection grants/actions. Audit may retain only the contracted redacted identifiers, states, counts, timestamps and usage facts; those facts can never be replayed as commands. The production management deployment must not register an endpoint or dependency for any forbidden owner, and an outage may block a fresh authentication or managed model/search lease but may not lock or rewrite an already accepted local workspace.
- Skill Hub and Share remain separately deployed, authenticated, user-initiated product data services rather than enterprise administrator/control-plane powers. Their catalog, publication, package, share-object, intent and receipt records never authorize local installation, enablement, execution, Session mutation, or content access by an administrator; the explicit local user action and pinned Harness owner remain mandatory.
- The product ships as the existing `anywhere-labs/deepseek-harness-desktop`-derived Electron/Cordis desktop base. The packaged process boots one in-process Harness Host and its loopback Web surface; e-Mate must not introduce another shell, launcher service, Host, Agent loop, session/event store, transport, or updater protocol.
- The browser frontend is responsive from 320px. Platform window behavior stays in the existing Desktop adapter rather than leaking into Profile plugins.
- Composer capability discovery uses the pinned client `InputTriggerService` as its only registry, palette and draft lifecycle. e-Mate 2.0.15 exposes one visible native `@` entry; File Import, Computer Use, native Goals/Todo plan, Schedule and enabled Skill owners contribute structured references through that service. The visible slash entry/menu is absent, while manual legacy slash text may remain parser-compatible for this version. Goal references use the native Goals API/projection, Plan references use the session-owned `todo_write`/`todos` projection, and no Shell Mention registry, popup, draft state, Goal/Plan store, Router or Session log may be added.
- The central chat UI derives activity from the existing DSH chat snapshot and plugin render metadata; it contains no second event store, transport, or tool-name dispatch table. Agent-authored natural-language progress, results, failures, images and interruptions always stay on the pinned DSH assistant renderer and keep its streaming state. The first-party Shell may shadow `assistant-step`, `tool-call`, and `context` only to collect Reasoning/Think, Tool and Context detail into one default-collapsed “运行过程” row per turn; expanded detail must delegate to the priority-0 DSH renderers and the existing `tool.call.toolview` winner, including GenUI. It must never aggregate, hide, delay, or rewrite natural assistant messages. Raw calls, results, and errors remain in the durable DSH transcript and audit/diagnostic surfaces; presentation folding must not delete or rewrite them.
- Terminal image presentation is one typed projection of durable `emate/image-output` receipts onto the owning native Assistant step/Conversation location. It deduplicates only by Attachment ID and may aggregate completed, review-required and failed image items into one accessible responsive gallery without scanning adjacent DOM, hiding another renderer, moving nodes or creating a second result store. GenUI continues to own only its `dsh-ui` Tool/client content and lifecycle; streamed prose, GenUI and the image gallery may coexist exactly once.
- WebUI-to-CLI communication must reuse the pinned Harness `ApiProxy` carrier and Web client architecture: typed HTTP `POST /api/<method>` and `/api/respond` uplinks, the two existing downlink-only WebSockets at `/api/events.mux` and `/api/events.host`, `dsh.client` modules, Cordis services, and registered UI slots. e-Mate may skin or compose those services but must not introduce a parallel protocol, event store, session state machine, or tool dispatcher.
- Main application source uses the pinned target's TypeScript/TSX and `tsdown` toolchain. Published JavaScript is a build artifact, not a second source implementation.
- Every non-chat page, component, token, brand asset, capability/settings surface, and responsive state must be a high-fidelity browser adaptation of task `019ff91c…`. The conversation surface alone follows the high-fidelity upgraded prototype `019ff665…`; an upstream Harness onboarding, empty state, or visual default is not an acceptable substitute.
- One Shell token owner maps light, dark and system colors for the workspace, titlebar, sidebar, surfaces, rules and interaction states. The Desktop adapter projects the resolved workspace/titlebar colors without changing Renderer ownership. Ordinary Settings hides engineering/plugin/provider/credential/concurrency surfaces by stable route or section metadata while `/capabilities` remains the user-facing status and explicit-action surface; hiding a setting must never unload its component. The Home composer and its workspace hint share one real semantic frame host, not a negative-size patch, ancestor guess or document scan.
- Historical e-Mate `v0.x`/`v1.x` screenshots, screenshots from pre-2.0.4 code, and superseded UI variants are forbidden visual references. The current `desktop/src/v1` component tree, current Token/assets, and final 2.0.4/2.0.5 acceptance state are the only non-chat visual baseline; an older screenshot cannot override them.
- First use requires authenticated acceptance of the e-Mate user agreement and the 亦芯 enterprise disclaimer. Only an enterprise-server receipt for all required document hashes unlocks the workspace; a local/browser flag is insufficient. Agreement archiving remains part of `emate.identity` and cannot expand enterprise control over the local runtime.
- The e-Mate 2.0.5 Skill Hub remains part of the capability center: authenticated users can publish a validated Skill ZIP, other users can discover immutable versions, verify/download the package, and install it into the local Harness Skill provider. A central administrator or catalog response cannot remotely enable, disable, inject, approve, or execute a local Skill; only an authenticated user's explicit local action may mutate that user's Skill state.
- Online task sharing is an explicit local user action over the pinned DSH Session ZIP, including its native descendant and attachment projection; it never creates another transcript, event store, attachment store, or browser upload path. The single loopback `/emate.share` Host adapter authenticates through the existing short-lived Model Gateway session and publishes only to the dedicated `emate-session-shares` R2 bucket. Public links are opaque, expire after seven days, and remain listable and revocable only by the exact authenticated owner and session after a client restart or uncertain create response. Local ZIP export remains a separate offline action, the release bucket is never bound to this service, and enterprise administration gains no local-session control.
- Online update and the complete Skill Hub lifecycle are Agent-callable product operations. Natural-language update requests such as “检查更新”, “更新插件”, or “更新 e-Mate” invoke only the typed `e_mate_desktop_update` Tool and its one native Desktop update service. Natural-language Skill requests may search/detail, install, update, enable, disable or uninstall another user's shared Skill, and may upload, publish or tombstone-delete a publication owned by the authenticated user. These actions use one typed Skill Hub Tool/Job transaction owner plus the pinned Harness Skill provider; the browser only projects real Tool/Job/session events. There is no browser keyword matcher, second intent engine, shell command, npm/pnpm path, duplicate Tool, or parallel lifecycle protocol.
- External-connection setup is device/account-global while the initiating Job and cancellation remain Session-owned. The only exception to Skill Hub ownership is an exact deployment allowlist of connector bootstrap Skills: native `skill_install` confirms the exact source, forces the DSH global managed root, and promotes legacy matching temporary installs before provider registration; every other source fails before network or disk mutation. Provider-native state remains authoritative (`~/.lark-cli` plus OS keychain for Feishu, DSH Credentials/settings for managed MCP and IM connectors), and every workflow must check existing status before setup. A new Session cannot by itself trigger `config init`, OAuth, QR pairing, secret rotation, or another consent prompt.
- A direct user instruction is consent only when it names one exact action and an unambiguous Skill slug/version or owned publication. Otherwise the Agent must use native `UserQuestions` before any network or disk mutation and show the exact source, version, digest and scope. Local install/update/enable/disable/uninstall commits only after the pinned DSH provider can read the exact candidate and `dsh-tool-skill` can load it. Upload/publish/delete requires authenticated ownership and an idempotent server receipt; delete is a tombstone over an immutable owned publication, never an overwrite of published bytes. Unknown completion remains recovery-pending and is reconciled after restart rather than reported as success or cancellation.
- The updater checks a signed compatible Profile generation before checking a Base installer. The Agent reports only the Tool result; the native confirmation displays the signed release version, changed component ids, compatibility decision, and remaining download bytes. A compatible component update rechecks the exact generation after confirmation, downloads only missing content-addressed bytes, stages an atomic restart generation, and commits only after Renderer health. An incompatible release checks the existing Base lane and otherwise returns `base-required`; it never attempts a speculative plugin install.
- Project memory is scoped by the pinned Harness `WorkspaceRegistry` identity and its canonical workspace path. Memory, dream distillation, learning output, and imported CowAgent material for workspace A must never be read from or written to workspace B; an ungrouped session receives session-local scope rather than a shared fallback. A missing, moved, or mismatched workspace binding fails closed.
- Every visible interactive UI element must complete a real Harness or enterprise-boundary action and expose its pending, success, failure, cancellation, and reloaded state. Empty handlers, placeholder links, fake success, permanently inert buttons, and visual controls with no reachable terminal result are release blockers.
- Permission domains remain separate. A DSH sandbox preset controls only the effects defined by the pinned sandbox policy; `danger-full-access` does not grant macOS TCC, Computer Use application leases, the CDP plugin's independent browser-control setting, credentials, network accounts, Skill installation, MCP process persistence, or another plugin-owned capability. The product baseline enables the endpoint-bound CDP setting and lets the user revoke it; this default remains independent of Full Access and never bypasses Chrome's own remote-debugging switch. The approval policy is a second independent domain: `approval/policy: never` rejects operations that require `ctx.approval` and must never be interpreted as automatic consent. Each plugin must use the pinned native authorization seam for its own resource and fail closed when that authorization is absent.
- Each Profile component declares one closed-vocabulary `authority_contract` (`effects` plus `guards`) in its package metadata; the deterministic emitter binds the exact same object into the signed component manifest and the Desktop parser rejects schema or vocabulary drift. The contract records and discloses authority but grants none: Cordis Host JavaScript is trusted in-process code, and sandbox, approval, plugin settings receipts, credentials, OS privacy grants, and authenticated remote accounts remain the only runtime authorities.
- Identity begins with self-registration, not administrator-created credentials. Registration requires a unique account, mandatory real name, a 10–256 character password, and a one-time expiring enterprise image-verification challenge. The enterprise server rate-limits challenge issuance and submission; successful submission creates only a `pending_approval` record and never a login lease.
- An administrator may list, inspect, approve, edit, disable, and soft-delete identity records through the existing management workspace. Approval is invalid until a positive per-user weekly Token limit and a valid model policy are configured. Disabled/deleted users and users without that allowance cannot log in; changing status, allowance, password, or deletion state increments the auth revision and revokes active leases. These operations remain inside `emate.identity`/`emate.modelPolicy` and grant no local-runtime control.
- Login carries an explicit `remember_login` boolean. It changes only the server-issued lease lifetime; the browser never persists a password, bearer, refresh token, or model credential. With it disabled, closing the authenticated browser context or ordinary lease expiry requires login again; with it enabled, the Host may restore an unexpired OS-keystore-backed lease after browser restart.
- Password login, logout, and password change retain the existing e-Mate request/receipt contracts. A successful change revokes prior account leases, returns `reauthentication_required`, rejects the former password, and permits a new login only with the new password; plaintext passwords never enter browser persistence, logs, audit, or receipts.
- Usage acceptance reconciles provider-reported immutable usage facts, the local durable audit outbox, the enterprise receipt, account counters, and the existing usage panel. Duplicate delivery is an idempotent no-op; a missing or conflicting fact is surfaced as a data-quality failure and cannot be hidden by an aggregate total.

## Model policy

| Public id | Provider model | Reasoning |
| --- | --- | --- |
| `ecorex-chat` | `gpt-5.6-luna` | `max` |
| `ecorex-gpt-5.6-sol` | `gpt-5.6-sol` | `medium` |
| `ecorex-deepseek-v4-pro` | `deepseek-v4-flash` | `max` |
| `ecorex-doubao-seed-2.0-pro` | `doubao-seed-2-0-pro-260215` | `medium` |

Gemini is not part of the 2.0.9 production catalog while its upstream route is unavailable. It must not be emitted by identity policy, shown in the client catalog, or enabled by default; restoring it requires a separately verified upstream route and release slice.

Image generation adapts the final e-Mate 2.0.5 Codex-like `imagegen` Tool: each call accepts only required `prompt` and optional exact current-session `image_url` Attachment IDs, produces one independent output, and uses separate target Tool calls and child Sessions for concurrency. `imagen` and Chinese image intents are lexical discovery aliases for this one schema, never second executable Tools or Providers. Agent-scoped health and source resolution fail with typed reasons before Provider execution rather than falling back to Skill Find, browser or CDP. Native Agent/Job admission is the only concurrency owner, accepts only the tested 1/2/4 capacity stages, and has a hard ceiling of four; the shipped default requires installed success, latency, rate-limit, memory, cancellation and unknown-result receipts. Generation and image editing are both fixed to `gpt-image-2-pro`; neither the user nor the Agent can select or pass an image model or provider. Only the enterprise image service may apply the verified 2.0.5-compatible `gpt-image-2-pro → gpt-image-2` fallback for eligible upstream-unavailable errors. The tenant policy may allow several chat models, while one session selects one active model.

The pinned rc.7 Session format remains unchanged. Harness commit `4787caf39134df190105b272da0dd2ba893d4d75` natively owns the downstream-event `ignorable: true` writer and reader contract; e-Mate must not patch or register any Session or Session-persistence seam a second time. `emate/image-output` is the only e-Mate producer and must set that marker. Unknown unmarked events continue to fail closed; no legacy event allow-list is injected into the native reader.

## Shipped plugin roster

The product-owned built-in list contains image generation/editing, Office, OCR, the DSH-native CDP browser adapter, Feishu, Tencent Docs, WeChat, and DingTalk. The deleted extension-based `dsh-browser` and its side panel are not part of the product. The capability center also keeps the community Skill Hub and installed custom Skills as a visibly separate catalog. Schedule, memory, dream distillation, and autonomous learning are local system plugins rather than extra built-in cards.

GenUI is a first-party portable Profile component and Vision Toolkit is a first-party target-specific platform Profile component in the accepted Desktop set, not ecosystem packages hidden inside the Base. GenUI retains the exact pinned upstream Tool/client bundle. Vision registers its upstream DSH Tools, Settings, Skills, subprocess and Web surfaces from one component; each signed target artifact carries the complete offline CPython wheel closure, while the Desktop exposes only the versioned `@e-mate/desktop/vision-toolkit` path to its packaged platform Python. Reintroducing `@omdsh-dev/dsh-genui`, `@anionex/dsh-vision-toolkit`, a Desktop-owned Vision loader row, an online runtime dependency installer, or another model/credential route is a contract failure.

e-Mate 2.0.12 additionally admits `@e-mate/dsh-plugin-glass-composer` as a portable Profile component. It may style only the Shell-owned semantic `data-emate-composer-frame-host` boundary that contains the native composer and its workspace hint, owns one DSH Settings namespace and one `conversation.input.right` control, and must not guess an ancestor from the control, inject the index, scan the DOM, store browser-local state, blur the composer, or replace its input lifecycle. The candidate Base id is v6 because its exact runtime-import ABI differs from the published v5 contract; reusing v5 would permit an incompatible component update. `@e-mate/dsh-plugin-idesign` is retired from the accepted set: its package, vendored archive, source/build inputs, profile bundle row and installed directory must all be absent, and profile reconciliation must remove any prior installation so it cannot remain as a ghost component.

Vision is enterprise-policy managed and ready without a user configuration page, so the ordinary e-Mate Settings navigation hides its native Settings section while retaining the component's Tool views, paste input, artifact presentation, health, Skill, subprocess and Host runtime. A hidden product setting must never be implemented by removing the Vision component or its native client graph.

Every user image introduced by paste, drop or upload follows Native Attachment First: before any model adaptation, the pinned Composer commits accepted bytes through the Harness Attachment/CAS owner and the durable user message carries the corresponding image `ContentBlock`. Vision must not suppress that native event or replace an image with a browser/workspace absolute-path sentence when the session-selected model is image-capable or its capability is unknown. Only a positively identified text-only model may derive a visual description at the LLM request/wire boundary from those existing Attachment bytes; the stored transcript, attachment identity, order and reload behavior remain unchanged. Capability comes from the selected Session model and catalog metadata, never model-name, UI-label or DOM heuristics, and intake never auto-switches the model or falls back to Skill Find, CDP or another Attachment store. In a mixed image/PDF/DOCX intake, native images and File Import each retain their existing owner and every input is admitted exactly once.

Internet search uses only pinned rc.7 `@deepseek-ai/dsh-web-search-deepseek` through the native DSH Web/Tool/Credentials seams. The e-Mate Profile fixes `web.searchProvider=deepseek-official`, the exact official `https://api.deepseek.com/anthropic/v1` endpoint and `deepseek-v4-flash` model, keeps `web_search` directly visible through Tool Search, and resolves the search-only device-global `E_MATE_SEARCH_KEY_DEEPSEEK` projected by the authenticated enterprise model policy; DeepSeek chat retains the distinct `E_MATE_MODEL_KEY_DEEPSEEK` ref and the two credentials may never alias or substitute for one another. Search must not install a Skill, ask for per-Session setup, add another provider/dispatcher, or expose the credential to the browser, Agent, logs, Settings, or component manifests. `@e-mate/dsh-plugin-search-mcp` and legacy `dsh-search-mcp` are retired from accepted inventories, generated bundles and repaired installations. Every authenticated approved 2.0.12 user negotiates an independent managed search grant on the existing runtime-policy endpoint, regardless of whether DeepSeek chat is in that user's model allowlist; `granted` atomically projects the OS-keystore credential, while `denied` or `unavailable` removes it without removing otherwise valid GPT chat routes. The no-query 2.0.11 response remains the exact legacy schema, the enterprise Gateway must be deployed before the 2.0.12 client, and managed model/search credential refs may never fall back to process, project, or user environment values. Missing, revoked, malformed, or unavailable search authority fails closed rather than falling back to an ungoverned credential or provider.

External connections are projected as the capability center's `collaboration` / “外部连接” subtype. The composer entry routes to that subtype through the existing browser History projection; it must not reopen the legacy Settings connector view or add another Router. `xmanrui/dsh-im` is adapted only from an exact audited commit and only channels with a complete licensed runtime, pinned-Harness compatibility, project/session binding and real authorization evidence may become ready. Any channel that lacks one of those proofs remains explicitly blocked; an `UNLICENSED` dependency cannot enter the release closure.

At the pinned Harness commit, only `web_search` is active from the relevant built-in set. `web_fetch` is shipped but disabled without a composed provider, and Office/PDF, OCR, and real Chromium Computer Use Tools are absent. e-Mate keeps thin adapters for those gaps and must still route them through target Tool, Job, attachment, subprocess, event, and client contracts; this finding does not authorize a parallel runtime.

CDP is the first and default path for every webpage read or operation. It uses the fixed loopback endpoint and Chrome's own remote-debugging security switch, contains no extension bridge, and ships its endpoint-bound browser-control setting enabled and revocable. Computer Use uses the pinned plugin's independent `allowAllApps` or exact per-application grants and its native interactive lease path, but its progressive Tools may be exposed or executed only when the latest direct user message carries the Shell's exact `@电脑操控` marker; the model cannot self-enable it and an earlier turn cannot authorize the next one. Candidate availability comes from Host runtime/platform/helper/permission state rather than browser UA: supported macOS may be ready or require permission, while Windows remains visibly unavailable with a reason until an exact native implementation has installed proof and must never be projected as executable. The e-Mate Full Access preset must not be mapped to either configured application access or an approval outcome. `find-skill` is discovery-only; all local and remote Skill mutations are owned by the Skill Hub transaction described below.

## Compatibility and delivery

### 2.0.14 capability-routing and Desktop bootstrap hotfix

2.0.14 is a single-purpose hotfix. The existing first-party `imagegen` and `image_pack` Tools are directly visible while long-tail Tools remain progressively disclosed; ImageGen leaf Agents keep the native `toolFilter` allowlist and call `imagegen` directly instead of discovering a Skill or browser path. The existing Find Skill component may invoke packaged pnpm only through Desktop-owned direct Electron-as-Node launch inputs, never by raw-spawning a `.cmd` or shell wrapper. Every e-Mate-owned Renderer reads its immutable Main/Preload bootstrap before URL or legacy session-storage fallbacks; external pages remain outside the desktop client graph. The macOS Helper selects the exact request envelope before validation: only predecessor 2.0.12 may use the exact 15-field legacy request and legacy swap/ACK/rollback path without a pending lease, while 2.0.13 and later remain on the bound request/pending/IPC path. Legacy target version is never hard-coded; request, staged bundle, running App and ACK must agree. These repairs do not authorize another capability router, launcher, Renderer store, Session, Agent, Tool registry, credential path, updater, or any unrelated 2.0.14 feature.

### Base and Profile component boundary

- “Everything is a plugin” means product behavior is delivered through ordinary DSH Profile bundles, client slots, and generated assets. It does not authorize changes to the pinned Harness source, a second plugin loader, a second update service, or Desktop-owned platform lifecycle code inside a product plugin.
- The Desktop base is the immutable native carrier: Electron, the pinned Harness/runtime closure, the existing Desktop startup and shutdown lifecycle, updater trust root, native helpers, and platform packaging/signing inputs. Browser behavior belongs to the CDP Profile component, not the Base. A Base release retains the 2.0.10 macOS arm64 interactive-shell, x86_64/Rosetta native-readiness, Windows renderer-health, closure, signature, clean-start, and warm-start gates.
- Base v7, its SDK receipt, every compatible Profile component and the public Desktop release manifest bind `schedule_protocol_floor: 1`. The updater rejects a missing, invalid or lower floor before requesting or staging payload bytes, accepts a higher positive floor only through a newer signed Base, and rechecks the exact floor and Base identity after user confirmation.
- Installed-app startup is gated against the accepted 2.0.11 installed artifact, not an absolute ceiling. On the same machine and architecture, use the same install/copy state, authentication/Profile state, launch command and probe boundary for one clean launch and three warm launches of both artifacts; for the clean sample, warm p75 and warm maximum, `candidate - paired 2.0.11 baseline` must be at most `10,000 ms`. An unauthenticated launch reaches the real visible, AX-readable, clickable login form at the paired probe boundary; its AURA renderer is statically bundled and eagerly mounted with the login route, never fetched or dynamically imported at login time, and failure or delay of WebGL cannot block identity input.
- An official Profile component is one immutable package or product Profile layer with its own id, version, byte count, SHA-256, provenance, and exact compatibility declaration. Third-party Profile dependencies and bundles remain a separate user-owned overlay; an official update must not install, remove, enable, disable, reorder, or upgrade them.
- A portable Profile component has `target: null`. A platform Profile component is independently releasable only when the inventory, component manifest and signed desired state bind the same exact tuple: `platform`, `arch`, native `runtime_abi`, `minimum_os`, signing scheme/identity and sorted native closure. The authoritative matrix is `darwin-arm64`, `darwin-x64` and `win32-x64`; each target builds and tests on its matching runner and emits only that target's native bytes. The client selects a platform/architecture desired state before download and rejects a mismatched tuple, native closure, binary format or signing declaration. `minimum_os` records the native capability floor; a capability below that floor stays unavailable without blocking unrelated portable components in the same generation.
- Compatibility is an explicit tested contract id, never an inferred SemVer range. A component manifest lists the exact accepted Desktop base contract ids and Harness contract id. For example, a component tested only with an rc.7 base is not offered to an rc.6 base. If the installed base is not explicitly listed, update selection must choose a compatible Base release; if none exists, it fails closed without staging the component.
- The public desired-state manifest is signed by the base trust root and binds every component plus the required base contract. Downloads are commit-scoped immutable objects and are verified by URL scope, size, SHA-256, signature, manifest identity, and package identity before staging.
- An official update stages a complete candidate Profile generation on the same volume, preserves the third-party overlay, and changes no active bytes. The explicit Agent request is the first confirmation and authorizes only signed metadata/manifest inspection. A separate native confirmation displays the exact delta; only then may component payloads be downloaded, reverified, staged, and followed by an orderly automatic restart. Activation is one atomic generation switch and becomes committed only after the existing renderer-health boundary. Failed activation restores and reopens the last-known-good generation. Background prompt deduplication is keyed by the existing update state's `(current generation, target generation)` pair, so a rollback to bundled or another generation offers the same signed recovery target once again without repeatedly prompting when both identities are unchanged. The application never downloads component payloads or restarts without that native confirmation.
- Local, dirty, unpackaged, temporary, Computer Use and loopback candidates are never production update inputs. They use isolated user-data/DSH homes plus loopback or fixture metadata, hold no production signing/R2/Feed/desired-state write authority, and cannot upload or activate public Desktop/Profile/component/update bytes. Production accepts only the exact protected-main CI artifact set whose source/run/target/bytes/hash/signature and install/update receipts all match the frozen candidate; a local path, copied bundle, symlink, dev signature, fixture receipt or rebuilt byte fails closed before the first production write.
- Plugin-only admission is allowed only when the repository impact classifier proves that all changed production inputs belong to independently packaged compatible components and that no shared input changed. The lane builds, tests, composes, and publishes only those components plus the final desired-state manifest. It must not build, relabel, upload, or revalidate Desktop installers or unrelated components.
- The accepted Base SDK is a content-addressed, manifest-verified artifact of the pinned Harness ABI, its clean-source build receipt and emitted `lib` bytes, the Desktop runtime/client carrier, and non-component Profile inputs. The Base lane builds Harness once; Desktop deterministically materializes those exact emitted libraries into its Yarn-installed packages and applies only the admitted e-Mate overlays, so Electron never falls back to the registry rc.7 implementation for a changed package. Platform jobs restore that same SDK and may not rebuild Harness. Cross-workflow Profile bootstrap must download it by the explicit `ci_run_id` and tested source SHA. Actions cache is only a same-CI performance optimization and is never release evidence. A missing or invalid SDK, tracked or untracked Harness source drift, receipt mismatch, unadmitted overlay, or resolved-package hash mismatch fails closed. Each changed component owns a frozen `pnpm-lock.yaml`, is installed outside the root workspace from that lock, and carries the lock plus its signed runtime closure in the component payload. Only the fixed Base ABI imports declared by the component contract may resolve from the Base SDK.
- Before a changed component can be published, CI merges its immutable payload with the complete currently accepted component set independently for `darwin-arm64`, `darwin-x64`, and `win32-x64` and materializes every referenced byte. When all changed payloads are portable, one macOS runner verifies all three target closures and boots the shared real Cordis Host/Web Loader graph once on arm64; unchanged platform components retain their already accepted native execution receipts. A changed target-bound component instead keeps the matching native runner and complete-generation boot for every affected target. Candidate artifacts retain only signed composition metadata; the temporary full store is verification state and is not forwarded as a release payload.
- The release workflow accepts only already successful CI evidence for the exact classified lane. A protected squash merge may promote the same immutable pull-request artifacts only when GitHub reports that exact PR merged into the current `main`, the current commit has the PR base as its sole parent, and the tested PR tree SHA equals the current `main` tree SHA. The release receipt keeps both the tested source commit and rewritten `main` commit. Any other merge shape or tree drift must run `main` CI. Normal plugin publication performs no build or test: it production-signs the admitted payload, uploads only missing immutable changed objects, performs authenticated and public size/SHA-256 readback, rechecks the parent desired state, then replaces the target's single `no-store` desired-state object. The one-time 2.0.11 bootstrap may build all accepted component targets from the already accepted Base SDK and must pass the same three complete-generation boots; it still does not build an installer.
- Therefore a source/test change inside one compatible portable component normally runs that component's build/test plus one three-target materialization and one shared-graph boot, then publishes only that component delta. A test-only change emits no release bytes. A target-bound component keeps its native target matrix. DMG/EXE, npm carrier clean-install and native Base acceptance rerun only when the classifier selects the Base lane or an explicit formal carrier release is dispatched; workflow or version labels cannot force a component change into or out of that lane.
- Changes to the pinned Harness pointer/source, Desktop runtime/API/updater, Electron or native helpers, permissions, shared Profile/core layer, the root or Desktop dependency locks, packaging/signing inputs, component format/trust rules, or an incompatible component contract enter the Base lane. A component's own dependency lock remains that component's input and may use the plugin lane. Unknown paths and incomplete provenance enter Base; there is no permissive fallback.
- CI and release workflows consume the same checked-in classifier result and finish through one aggregate required check. A workflow-local path list, manually selected faster lane, or version label cannot override the classifier.

- Import only authoritative, non-deleted old e-Mate/ECoreX sessions; preserve sources unchanged and make import idempotent. These roots are optional read-only sources, never a startup or current-Session dependency: missing, removed, temporarily unreadable or discovery-raced sources degrade to a clean no-op with at most one redacted Host-lifecycle diagnostic and no sidebar/Session error projection, while integrity, identity and target-persistence failures continue to fail closed.
- User data stays under the resolved `$DSH_HOME`; npm installation directories are read-only code/resources.
- Supported release platforms are macOS 13+ arm64/x64 and Windows 10/11 x64.
- Keep the existing download, admin, audit, and public R2 origins unchanged. Published Desktop installers and Profile components use version-and-commit-scoped immutable keys; all prior objects remain frozen and are never overwritten, and the application server must not become the binary origin.
- A Base release requires clean native installation on every selected platform, automated closure checks, interactive startup/renderer health, affected Computer Use acceptance, atomic update and rollback proof. Performance evidence is optional diagnostics. A plugin-only release instead requires the changed component tests, composition with the accepted Base/Profile generation, restart/health/rollback proof, and immutable public receipt; it must not rerun unrelated native installer acceptance.
