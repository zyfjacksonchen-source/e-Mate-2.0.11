# e-Mate 2.0.7 开发切片合同

本文件是切片边界和退出条件，不是完成声明。实际状态以 [`../development-log.md`](../development-log.md) 的带日期证据为准；没有真实平台、账号、凭据或服务器回执时保持阻塞，不用 fixture 代替发布验收。

## 状态总览

| 切片 | 当前状态 | 主要未关闭项 |
|---|---|---|
| S00 基线 | 当前 CI 与 tarball 构建通过，待 npm 回读 | npm 正式发布后的 registry 身份回读 |
| S01 依赖闭包 | 单 tarball 三平台候选通过，平台能力阻塞 | Windows Edge 与 macOS Ego 真实门禁 |
| S02 浏览器外壳 | 核心路由、五断点、明暗模式及复制/消息队列/活动组折叠/重试/失败/长文本已验，整体仍进行中 | Tool 排队事件、其余原型状态、产物同状态与上游编辑/失败轮重试动作 |
| S03 真实事件聊天 | 持久终态、审批、重试尝试链与刷新恢复通过，生产链路阻塞 | 真实 Provider 流、模型切换与受控弱网恢复 |
| S04 性能 | 本地 30 对 Harness 门禁通过，生产阻塞 | production Profiler 与真实 Provider 首响/生成/Tool 配对收据 |
| S05 CLI/快捷方式 | macOS 单实例、关闭浏览器后恢复与安全 stop 通过 | Windows 实机 `.lnk`；系统默认浏览器快捷方式可见证据 |
| S06 安装升级 | 同版受管事务通过，跨版仍阻塞 | 正式 npm 跨版、失败恢复与降级矩阵 |
| S07 企业插件 | 生产管理员、模型联通与 Usage 对账通过，普通用户流程仍待验 | 新用户注册/批量审批、周配额、保持登录、改密与租约断连组合验收 |
| S08 会话迁移 | 15 个真实旧会话可见、刷新恢复与源库不变通过 | 真实 Provider 下继续对话、项目移动与旧账号映射 |
| S09 系统插件 | schedule 页面与项目记忆隔离合同通过，历史数据仍待验 | 真实历史 schedule/项目移动样本与迁移收据 |
| S10 内置能力 | 图像上游单次/改图/双并发通过，其余执行层阻塞 | e-Mate Tool 端到端、稳定并发上限、Office、Vision/OCR、双端 Browser |
| S11 外部连接 | 四类连接可发现并到达真实授权 handoff | 按 2.0.7 验收边界不提交 OAuth/扫码；发布候选复跑 |
| S12 组合验收 | 主代理逐项执行中 | 生产登录、模型/弱网/性能、能力执行层、升级与对账 |
| S13 发布覆盖 | 管理端与 Usage 生产 URL 已激活，包发布仍阻塞 | npm 回读、R2 admission、下载 URL 与平台能力验收 |

## S00 基线

- 输入：独立仓库 `zyfjacksonchen-source/e-Mate`、MIT、版本 `2.0.7`、Harness `0.1.0-rc.5` 与提交 `47f943859bef60e4160492346772ded9b24f765a`。
- 修改边界：只建立产品包、上游只读引用、目标锁和开发记录；不得改 Harness 核心。
- 检查：`pnpm check:target`、`pnpm --filter @e-mate/dsh pack`、仓库外执行 `e-mate --version`。
- 退出：包名、产品名、版本、上游提交和许可证在源码、tarball 与运行输出完全一致。
- 依赖：无。

## S01 依赖闭包

- 输入：单一 `@e-mate/dsh@2.0.7` tarball、固定 Harness 收据和十个内嵌 bundle manifest。
- 修改边界：删除六个平台 Runtime/Browser 包及旧 Python/Office/OCR/Chromium 预装；用户安装阶段不得 pip install、下载浏览器或原生编译。
- 检查：bundle registry/入口/许可证、单包 SBOM/SHA、仓库外干净全局安装、profile dump 和真实平台 readiness。
- 当前证据：提交 `a52bc07aa5410c496b0731ab7c8840922a355c06` 的 Release run `31889821590` 从同一源码构建唯一 `e-mate-dsh-2.0.7.tgz`，生成 SHA/SBOM/许可证证据，并在 macOS arm64、macOS x64、Windows x64 三个干净 runner 上从同一 artifact 完成 npm 安装和 setup checks。npm 发布、registry 回读与 R2 job 按 PR 门禁保持 skipped；Windows Edge 与 macOS Ego 的真实 readiness 仍未验收。旧七包安装证据不继承为本架构的通过项。
- 退出：同一 tarball 在 macOS arm64/x64、Windows x64 可安装；Windows 未验收时准确返回 `PLAYWRIGHT_MCP_EDGE_UNVERIFIED`/`setup-required`，macOS Ego 未验收时同样失败关闭，且不发生浏览器二次下载。
- 依赖：S00。

## S02 浏览器外壳

- 输入：最终 e-Mate 2.0.4/2.0.5 提交 `564a6b6c1d43fb6831dd4a5cd8026e472f063311` 的页面、Token、组件和品牌资产。
- 修改边界：删除 Electron/窗口补偿；不改目标 Connection、Session、Workspace 或路由状态机。
- 检查：`/login`、`/`、`/chat/:sessionId`、`/capabilities`、`/settings`、刷新/前进后退/深链；320/390/768/1280/1920；明暗主题。
- 退出：全部主要按钮和表单闭环，无空入口、横向溢出、桌面窗口留白或浏览器不可兑现能力。
- 依赖：S00。

## S03 真实事件聊天

- 输入：任务 `019ff665-d721-79a0-869d-338f086cf529` 原型与目标持久事件/实时帧/注册表。
- 修改边界：只使用目标 `conversationEvents`、Conversation Node、turn data、turn-tail、answerable frame 和 Tool renderer；禁止私有 HTTP/WS、第二 Store、伪状态和 Tool 名分发表。
- 检查：完成/运行/审批/失败/取消/重试、重放/乱序/重连、长文本同 DOM、未知内容、图片、文件、编辑分支。
- 当前补充证据：固定 50-event Session（SHA-256 `e88c05a486d196cfd14f2ae8d0be003de191b826b1c226d803bbfb658ff15096`）在主代理 Browser 中真实重放 completed/error/blocked/aborted/interrupted/max-tokens 六个 `turn/end`。首轮发现 `interrupted` 被 cancelled fallback 显示为“已取消”；根因只在活动头状态映射，修复后六态分别保持“已处理/执行失败/已阻塞/已取消/已中断/已处理+输出上限提示”，终态不启动 running timer/observer，重复 turn-tail 继续隐藏。随后以相同 `retryId` 的两次真实 `llm/retry` 重放发现目标默认 View 只显示 `current`；e-Mate 仅通过目标 keyed-slot shadow seam 渲染 target 已投影的 `attempts`，现在第 1 次失败与第 2 次取消各自可见且可展开，未新增事件、Store 或 transport。证据位于 `../../artifacts/design-qa/S03-chat-states-current-46d306d/` 与 `../../artifacts/design-qa/S02-chat-prototype-current-dee4b39/`。失败卡的“重试”按钮仍失败关闭：rc.5 没有失败轮前原子分支/原消息与附件重放动作，不能用 `setDraft + submit` 伪装。
- 当前消息动作证据：主代理在同一 `1280×720` 暗色运行中实点目标 `MessageIconActions` 的复制动作，同一消息原位显示 check 与“复制成功”，约 1 秒后恢复“复制”，无全局通知或伪事件。源/实现聚焦并排图位于 `../../artifacts/design-qa/S02-message-copy-current-f225cc2/focused-source-vs-current-copy-feedback.jpg`。原型中的完成态消息编辑仍失败关闭：rc.5 没有 pre-turn fork 加原消息/附件精确重放或原子 edit-and-branch action，不能用追加重复消息冒充编辑分支。
- 当前排队证据：真实运行 turn 中从目标 InputBar 再提交一条消息后，权威 `session/queue` 产生 queued occurrence；主代理完成编辑保存、删除、重新排队后插话三个目标动作，均无重复或残留。证据位于 `../../artifacts/design-qa/S02-queued-current-7b607c7/`。原型第 6 屏展示的是 Tool 在 dispatch 前“等待运行 / 排队中”，但 rc.5 只在 `startCall()` 真正开始时追加 `tool/call`，没有前端可投影的 Tool queued 事件；不得把用户消息队列改名或提前造调用，该同状态继续阻塞于目标事件合同。
- 当前折叠证据：主代理用仓库外目标 Tool 保持一条真实 `tool/call` 运行态，发现共享活动头与原型第 3 屏在字号、内容宽、字重及 Chevron 上存在 P1；最小修复后最终 client bundle SHA-256 为 `63a69d3ec37c0cc6b661d7c88e7c0a79d414e0c7d163af2d834d83960bf2fd30`。同一真实活动组展开时 Tool 行数为 1，折叠后 `aria-expanded=false` 且 Tool 行数为 0，再展开恢复为 1；最终 `22px / 31.9px`、内容宽、`18×18` 向下 Chevron 与源稿同状态一致。证据位于 `../../artifacts/design-qa/S02-collapse-current-7b607c7/`。
- 当前摘要/图片证据：主代理以 13 个目标持久事件、真实 `read/glob/bash` Tool 调用和 CAS 图片附件逐屏复验原型第 4/5 屏；未引入第二 renderer。默认/刷新态为图片摘要收起，点击后显示同一 target ImageGallery 并可打开/关闭原图 lightbox，再点击恢复收起。最终普通消息、三类 Tool 摘要、图片摘要和单图几何与逐屏标注稿一致；`320/390px` 文档无横溢出，侧栏触发器与 Session title 各自保留 `44px` 热区且不重叠。证据位于 `../../artifacts/design-qa/S02-summary-current-60b0955/`，client SHA-256 为 `a2d29fe556d92b58919bdf3ced40e7ea2ca092a30b7f3aaefede03b09b765a68`。
- 退出：活动组、正常回答、产物区顺序稳定；状态和耗时只来自真事件；未知插件不崩溃不丢数据。
- 依赖：S02。

## S04 性能

- 输入：5,000 事件固定会话、30 事件/秒流、20 会话开关场景、Chrome Trace 与 React Profiler。
- 修改边界：优先目标已有缓存/selector/virtualization；只有测量证明瓶颈后才改实现。
- 检查：冷启动、INP、事件到绘制、FPS、长任务、heap、泄漏，以及与固定 Harness 同机器/同模型/同网络配对的首响、持续生成吞吐和 Tool 调度速度；同时验证企业端断连但有效租约/策略存在时旁路不拖慢本地运行。
- 当前证据：固定会话 `e-mate-performance-5000-v1` 含 5,000 个目标持久事件、625 个 turn，事件 SHA-256 为 `ad12feaa53f9d55c22d0e32a366316c8e29a8031ccca5d27a8e4f97d2a99b0cc`。单一 Harness 实例通过目标 history paging 稳定加载 4,999 个聊天行后，实测约 45,300 个 DOM 节点、97–104 MB heap、58.4 fps、稳态无 >100 ms 长任务，满足当前预算；掉帧百分比按用户修订仅保留为诊断数据，不再作为发布门槛。提交 `098f36c8cb769e7e8f8eeb6f6bd18bb98c030eff` 的当前 bundle（client SHA-256 `3495cdfa492f036d105de02c170891233e208e885f233d44034c8b963cae4457`）在隔离单实例中再次从 turn 601 加载到 turn 1，625 条用户消息和 625 条助手消息均真实投影，最终 DOM 为 39,345 nodes；24 个有效历史页的 wall time 中位数为 380 ms、最大值为 589 ms，随后一次边界请求移除“加载更早”。该 Browser 表面不开放 CDP Trace、React Profiler 预注入、heap 或 browser metrics，因此本次只补当前候选的真实分页/DOM证据，不能替代旧闭包的 Trace/heap/FPS，也不关闭发布门槛。React production Profiler 摘要与正式支持浏览器的最终候选重跑仍待补齐。完整历史结果见 `../../artifacts/performance/S04-2026-08-15-metrics.json`。
- 退出：满足 [`../performance-and-acceptance.md`](../performance-and-acceptance.md) 全部发布阻断预算。
- 依赖：S03。

## S05 CLI/快捷方式

- 输入：`setup/check/web/launch/status/stop/dump-config/version` 与目标 Harness 启动器。
- 修改边界：快捷方式只调用当前全局 `e-mate launch`；不绑定旧 npm 路径，不登录自启，不驻留托盘，不误杀端口占用进程。
- 检查：单实例、僵尸 PID、端口冲突、浏览器关闭、重复双击、健康接口和优雅停止。
- 当前证据：macOS arm64 已用上述单一 tarball 实跑 setup、快捷方式连续启动、同版覆盖、status/stop 和非受管端口冲突；两次快捷方式调用复用同一 PID/实例，端口占用进程未被停止。in-app Browser 关闭全部标签后 Host 仍健康，再开新标签恢复 `/login` 且 title 为 e-Mate。当前用户登录 shell 只读探测找不到全局 `e-mate`，所以真实 Desktop 快捷方式目前不能启动；没有用未发布本地包污染全局环境来伪造通过。仍待正式 npm 全局安装后补系统默认浏览器可见证据和 Windows `.lnk` 实机。
- 退出：macOS/Windows 真实桌面覆盖后均可恢复页面，实例 ID 与 PID 身份匹配。
- 依赖：S01、S02。

## S06 安装升级

- 输入：同版重装、rc 到正式版、跨版升级、活动任务、失败恢复和降级请求。
- 修改边界：Agent 和人工安装共用一个受管事务；先校验闭包，再快照/迁移/健康检查/激活。
- 检查：迁移幂等、凭据和数据保留、快捷方式覆盖、失败收据、回滚数据摘要、降级拒绝，以及用户用自然语言触发 Agent 复用同一受管 npm 更新事务并在恢复后核对版本/完整性收据。
- 当前证据：当前单包在隔离 macOS arm64 npm 安装后连续执行两次 `setup`，环境检查 8/8；第一次只读导入 15 条既存非删除会话，第二次 `imported=0/reused=15` 且 source fingerprints 不变，schedule 收据同样无重复导入。Updater Helper 会重新验证落盘 request ID/target/current version，并把 npm 验证的 installed/previous SHA-512 integrity 写入本地终态收据；正式发布工作流已要求三平台 npm 回读后执行一次同版在线更新并核对 request ID、终态、两份 integrity 和停止清理，但该远端门禁尚未实跑。跨版本失败恢复、活动任务实跑和降级矩阵仍未完成。
- 退出：任何失败不激活新版本且能从匹配快照恢复；成功后状态与包版本一致。
- 依赖：S01、S05。

## S07 企业插件

- 输入：`emate.identity`、`emate.modelPolicy`、`emate.audit` 与现有企业 API。
- 修改边界：只鉴权、模型策略和异步审计；不得出现插件/工具/会话/Job/本地运行控制 API。
- 检查：注册真实姓名+验证码、管理员审批/CRUD、保持登录、改密/退出再登录、多选模型、租约离线、outbox、周配额与 usage 对账。
- 当前证据：主代理用当前 e-Mate profile 直连既定生产 Auth Gateway，在未提交账号、姓名、密码或注册申请的前提下，真实取得并刷新两份不同的注册验证码；注册页包含账号、真实姓名、至少 10 位密码、验证码和“待管理员审批后使用”的既有合同，登录页包含“保持登录”。首轮 `320px` 验收发现底层 Harness `#root` 把文档撑到 `432px`；定点修复只在身份 Gate 存在时隐藏并隔离该底层根，卸载后恢复原 `hidden/inert`，主代理复验 `320/1440px` 均为 `scrollWidth === clientWidth`。该证据关闭公开 challenge、注册入口和响应式边界，不代表创建用户、管理员审批、协议归档、租户租约或账本对账已经完成。
- 当前生产证据：新租户独立管理员 `emate-admin` 已通过现有 Auth Gateway 账号密码登录；Analytics 逐请求验签 access session 并回查当前 ACTIVE 会话/角色。管理员免签只由签名 `TENANT_ADMIN` 角色决定，普通用户同意门不变。管理端 Luna 实时联通测试与 Usage 面板 7 日真实账本均已通过；Usage 显示 3 次入账调用、13,185 Token、用户周额度“不限”，四项对账差异均为 0，调用明细与汇总同一租户/时间范围。新用户提交注册、批量审批、配额/模型下发、保持登录和改密仍须另建明确验收用户后完成，不能由管理员样本替代。
- 退出：生产回执和真实账号通过；企业断连不阻断有效本地任务，新登录和过期租约失败关闭。
- 依赖：S02、S03。

## S08 会话迁移

- 输入：e-Mate/ECoreX/CowAgent 权威 SQLite/WAL、附件、记忆和收据。
- 修改边界：源只读；不从 UI cache 复活删除会话；直接写目标 SessionPersistence 格式，旧 Tool 只作历史证据。
- 检查：稳定 ID、去重、幂等、损坏关闭、源哈希不变、附件 CAS、项目绑定、继续对话。
- 当前证据：在隔离 `DSH_HOME`/Desktop 上读取本机 3 个真实权威来源；首次 setup 导入 15 条，第二次 `imported=0/reused=15`，3 个 source fingerprint 完全一致。源数据库、WAL/SHM、Runtime artifact、CowAgent 树及旧 memory 证据的聚合 SHA-256 在两次 setup 前后完全不变；全程未输出标题、消息、附件名或凭据。当前真实源没有 deleted 状态行，删除会话不复活仍由可执行 fixture 合同覆盖，不能冒充真实删除样本通过。
- 退出：真实非删除样本完整可见，重复运行不增量污染，项目 A/B 和记忆不串。
- 依赖：S03。

## S09 系统插件

- 输入：`memory-evolve`、rc.5 native Subagent receipt、官方 `@deepseek-ai/dsh-schedule` 和旧任务。
- 修改边界：`memory-evolve` 只写目标 Storage Domain 并绑定 WorkspaceRegistry/session；不保留旧 dream/learning 并行实现。旧任务默认 disabled，用户后续精确确认后才调用目标 Schedule Tool。
- 检查：copy-on-write、项目/通用会话隔离、evolve 行为、native Subagent durable events、任务幂等激活和崩溃对账。
- 当前证据：固定 rc.5 `WorkspaceRegistry` + JSON Storage Domain 的可执行组合测试已证明路径别名仍解析到同一 canonical workspace；同一“通用会话”工作区的不同 Session 使用不同 memory key，普通项目的会话共享项目 key，跨项目 key 不同。插件 4/4、受管 profile/九 bundle 闭包 2/2，源码、同步 bundle、tarball 与仓库外安装副本的 `scope.js` SHA-256 一致。
- 退出：真实样本源不变，跨项目不可召回，旧任务无未经确认执行。
- 依赖：S08。

## S10 内置能力

- 输入：图像插件及 `office-skills`、`search-mcp`、`ego-browser`、`browser-panel`、`vision-toolkit`、`genui`、`better-sidebar` 的目标合同。
- 修改边界：只使用固定 Harness Loader/Skills/Tools/Jobs/Session events/Connection/slots；不恢复旧 Python/Office/OCR Worker 或随包 Chromium，不建立第二通信层。
- 检查：生图/改图单次、并发逐级加压得到稳定上限并验证上下文/附件不串；四个 Office Skills 的真实创建/读/改/导出/重开；搜索；Vision/OCR governed route；macOS Ego 与 Windows `@playwright/mcp@0.0.78` + 系统 Edge 的交互/下载/隔离/清理；Browser Panel/GenUI/Sidebar 真动作。
- 当前阻塞：Vision/OCR 缺 rc.5 enterprise model-policy seam；Windows 缺 workspace-root/permission 组合和实机验收，状态必须是 `PLAYWRIGHT_MCP_EDGE_UNVERIFIED`/`setup-required`。本机只读扫描未找到 `ego-browser`/`ego` 命令或 Ego Browser 应用，macOS 继续准确返回 `EGO_BROWSER_RUNTIME_UNVERIFIED`/`setup-required`，没有自行下载或伪造平台通过。
- 退出：真实模型、外部工具和产物逐项通过，前端只投影真事件；安装过程中无浏览器二次下载。
- 依赖：S01、S03、S07。

## S11 外部连接

- 输入：飞书、腾讯文档、微信、钉钉官方授权/API、本地凭据引用，以及固定 Harness Credentials/MCP/Tool/Job/Connection/Settings/Storage Domain 合同。
- 修改边界：先以同一目标接口补 Keychain/CurrentUser DPAPI provider，再实现一个共享 TypeScript Cordis 连接插件；不移植旧 Python Runtime、SQLite Store、Dispatcher、mcporter、全局绑定或企业 Managed Connector Gateway。仅在许可证和官方条款允许时复用第三方代码。
- 切片顺序：OS 凭据 provider → 共享 Host 与 loopback RPC → 目标 Settings UI → 钉钉消息 → 飞书消息 Bot → 微信设备流 → 腾讯文档 credential-aware MCP → 动态业务 Tool/能力卡 → 组合验收。
- 检查：四类连接必须能被用户与 Agent 发现，进入正确连接页并到达真实 provider 授权 handoff；secret 不经 describe/RPC/log/Tool/config dump 返回。2.0.7 的发布验收在提交 OAuth consent、二维码确认、凭据或外部写入前停止；飞书文档/Drive、微信 iLink、腾讯文档 OAuth/MCP 和钉钉协议依赖仍保留独立许可证/授权门禁。
- 退出：四类连接的发现、路由和授权前置步骤均以 provider 真实页面/挑战通过，日志和前端无凭据值；不以静态卡、假二维码或近似页面替代，也不要求本版本提交真实授权。
- 依赖：S07、S10。

## S12 组合验收

- 输入：S00-S11 的单一发布候选 tarball、固定数据集、真实账号和 Computer Use 脚本。
- 修改边界：主进程执行验收；独立失败才交给子代最小修复，只有主进程关闭条目。
- 检查：安装/自然语言在线升级/快捷方式、身份/模型切换与上下文连续、弱网/上游恢复、usage、按 `019ff665-d721-79a0-869d-338f086cf529` 逐交互逐屏核对的全部聊天状态、内置能力与生图稳定并发上限、外部连接授权前 handoff、迁移、性能和负向边界。
- 退出：每个场景有日志、截图、Trace、产物、摘要与审计证据；无账号/授权项明确阻塞。
- 依赖：S00-S11。

## S13 发布覆盖

- 输入：单一主包 tarball、SHA256、SBOM、许可证、npm 账号和三个生产地址。
- 修改边界：顺序固定为 CI 构建主包、仓库外干净安装、Computer Use、npm 发布/回读、Cloudflare R2 同字节不可变对象与收据验收、下载页、管理/审计版本展示；下载页只作为入口和完整性页面，最终包字节必须来自 R2，不得提前激活 URL。
- 检查：npm registry 回读与 tarball SHA、干净全局安装、生产身份/模型/审计/usage 对账、URL 可达和回滚收据。
- 当前证据：提交 `a52bc07aa5410c496b0731ab7c8840922a355c06` 的 CI run `31889821600` 和 Release run `31889821590` 均为 `success`；后者已完成单一 tarball 构建、release evidence、SHA/SBOM/许可证校验，以及 macOS arm64/x64、Windows x64 三平台干净 npm 安装。npm publish、registry install 和 R2 publish job 均按 PR 门禁保持 skipped，因此没有 registry 回读、R2 admission 或生产 URL 激活证据；尚未使用发布凭据，也未修改任何生产 URL。
- 当前 URL 证据：`https://mvdcm.ecoremedia.net/ecorex-agent/admin/`、`https://dl.ecoremedia.net/ecorex-agent/admin/`、`https://mvdcm.ecoremedia.net/ecorex-agent/usage-panel/` 与 `https://dl.ecoremedia.net/ecorex-agent/usage-panel/` 均为 HTTP 200。Usage 静态页已从独立 Nginx Basic Auth 切到同源 Auth Gateway 账号密码登录，数据 API 继续要求 Analytics Bearer；旧 `/usage-panel/api/` 仍返回 401。下载 URL 尚未切到已验收 R2 对象，npm/R2 正式发布与下载覆盖仍保持 open。
- 退出：`@e-mate/dsh@2.0.7` 的同一字节从 npm 和 R2 可验证安装，三个现有 URL 展示同一活动版本并通过生产验收。
- 依赖：S12。
