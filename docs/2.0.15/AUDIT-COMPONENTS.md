# e-Mate 2.0.15 component and architecture audit

> Ticket: T03. Audit baseline: `b3773a72274b51032a39cc92d4b0b0db8dcd25ec`. This is a source-and-receipt audit, not installed acceptance. `fixture/source test present` never means `functional closure` below.

## Status rules

- `Keep`: the current owner and native DSH path are coherent; installed positive/negative evidence is still required.
- `Fix`: a concrete contract or implementation mismatch is present and is assigned to one ticket.
- `Merge`: no component currently has enough evidence to merge safely.
- `Delete`: remove the component from source and all future Profile closures; do not rewrite immutable releases.
- Priority describes the remaining 2.0.15 risk. All 16 rows remain `NEEDS_EVIDENCE` until the named real E2E is recorded.

## Inventory closure: 16/16

| ID | Inventory component | Unique owner | Decision / priority | UI or entry -> RPC/Tool -> Host -> Store/service -> result projection | Current evidence | Missing real E2E | Target ticket |
|---|---|---|---|---|---|---|---|
| C01 | `@e-mate/dsh-client-shell` | T13 | Keep / P0 | Header, sidebar, composer and session screens -> native client stores plus `/emate.identity`, `/emate.share`, `/emate.schedules`, `/emate.mcpManage` -> profile Hosts -> native Session/Workspace and enterprise services -> native shell projections | Native slot registration, route fencing and session operations have source tests; Shell declares no independent effect plane | Installed first-run, navigation, reconnect, rename/archive, composer and header hand-offs on all targets; T11/T12 may consume Shell surfaces but must not become a second Shell owner | T13 |
| C02 | `@e-mate/dsh-plugin-skill-hub` | T07 | Keep / P0 | Skill Hub UI/typed Tools -> `/emate.skillHub` and native Skill/Job APIs -> Skill Hub Host -> native provider plus Skill Hub Worker -> install/enable/call/status projections | Lifecycle, receipt, recovery and permission source tests exist | Two-account installed publish/discover/install/enable/call/disable, restart recovery, denied permission and unavailable Worker | T07 |
| C03 | `@e-mate/dsh-plugin-better-sidebar` | T16 | Keep / P1 | Sidebar entry -> `/emate.betterSidebar` -> Host -> `WorkspaceRegistry` plus bounded realpath file reads -> sidebar projection | Workspace scoping and bounded-read source tests exist | Installed multi-workspace positive flow, traversal negative flow and restart restoration | T16 |
| C04 | `@e-mate/dsh-plugin-cdp` | T15 | Keep / P0 | `@电脑操控`/browser Tool -> native approval and CDP Tools -> Host -> fixed loopback Chrome CDP at `127.0.0.1:9222` -> Tool result and capability health | Tool, lazy-launch, approval and settings source tests exist | Installed visible Chrome positive flow; no-browser, wrong port, cancellation and unauthorized invocation negatives | T15 |
| C05 | `@e-mate/dsh-plugin-computer-use` | T15 | Keep / P0 | Explicit `@电脑操控` -> Computer Use Tools -> Host/helper -> macOS Accessibility, Screen Recording and input services -> Tool result and capability health | Pinned upstream adapter, helper-closure and current-turn authority tests exist; component patch disables non-darwin runtime | Real macOS TCC positive and denial/cancel negatives; Windows must explicitly project unavailable and must never claim fake support | T15 |
| C06 | `@e-mate/dsh-plugin-file-import` | T16 | Keep / P1 | Composer attachment -> `/emate.fileImport` -> Host -> Session-to-Workspace mapping and atomic `.e-mate/imports` publish -> native draft/artifact projection | Payload, type, size, filename, workspace and atomic-write tests exist | Installed valid file/import-to-turn/restart flow plus invalid type, oversize and orphan-cleanup negatives | T16 |
| C07 | `@e-mate/dsh-plugin-find-skill` | T15 | Keep / P0 | Natural-language skill request -> find/install Tools -> Host/subprocess -> fixed connector catalogue and native Skill lifecycle -> discovery/install receipt projection | Allowlist, fixed-catalogue, receipt/migration and spawn-`EINVAL` source tests exist | Existing-auth and new-install Feishu flows; cancel/denial; Tencent Docs, DingTalk and WeChat with real accounts or explicit external blockers | T15 |
| C08 | `@e-mate/dsh-plugin-genui` | T16 | Keep / P1 | Tool output -> native ToolView/GenUI client -> pinned Host/client bundle -> native turn store -> rendered result card | Package-surface source test exists | Installed live Tool success/failure rendering and simple/detailed message-mode compatibility; current single package test is not closure | T16 |
| C09 | `@e-mate/dsh-plugin-glass-composer` | T16 | Keep / P1 | Native composer slot -> settings Host -> native DSH setting store -> glass decoration of the same semantic composer | Slot and settings source test exists; component is decoration, not an alternate composer | Installed persistence, reduced-motion/accessibility and proof that only one composer/input owner exists; retirement requires explicit migration evidence | T16 |
| C10 | `@e-mate/dsh-plugin-mcp-manage` | T16 | Keep / P0 | MCP settings UI/Tools -> `/emate.mcpManage` -> Host -> native settings/OS credentials/subprocess/OAuth -> active/status/result projection | Add/remove/status, credential and process-boundary source tests exist | Installed add/activate/status/remove/restart; invalid server, OAuth denial and inactive-tool negatives | T16 |
| C11 | `@e-mate/dsh-plugin-memory-evolve` | T16 | Keep / P1 | Remember/search Tools -> Host/system prompt -> native `storageDomain` scoped by Workspace -> Tool answer | Workspace scope, confirmation, remember/search and prompt source tests exist | Installed cross-session/restart positive plus cross-project isolation and denied-confirmation negatives | T16 |
| C12 | `@e-mate/dsh-plugin-office-skills` | T16 | Keep / P0 | Office Skill/Tool -> native Job -> Host -> Workspace/Sandbox plus DOCX/XLSX/PPTX/PDF libraries -> artifact/result projection | Round-trip, archive-bomb, workspace and Job source tests exist | Installed real four-format outputs, open/download projection, cancellation, malformed input and partial-file cleanup | T16 |
| C13 | `@e-mate/dsh-plugin-schedules` | T06 | Keep / P0 | Schedule panel -> `/emate.schedules` -> read-only Host -> native `SessionPersistence` and schedule change stream -> list/status projection | Read-only projection source test exists; schedule creation/execution remains native rc.7 ownership | Installed create/list/dispatch/delete/restart and failure recovery, with Tool Search coexistence and no duplicate scheduler | T06 |
| C14 | `@e-mate/dsh-plugin-tool-search` | T05 | Fix / P0 | Agent turn -> native per-Agent `tools.restrict` plus `tool_search` -> Tool Search Host -> native Tool registry and managed search provider -> restricted Tool/result projection | Late registration, child inheritance, failure behavior and routing source tests exist; the component patch currently overrides the profile search provider with insecure HTTP and a different model | Installed deterministic ImageGen/Schedule/Web Search selection, prompt/header receipt, late Tool registration and fail-closed credential/transport negatives after contract reconciliation | T05 |
| C15 | `@e-mate/dsh-plugin-vision-toolkit` | T15 | Keep / P0 | Vision Tool -> Host -> enterprise model settings, sandbox and target-specific CPython wheels -> OCR/vision Tool result and capability health | Native-surface, bounded-health and package metadata tests exist | Actual arm64/x64/win32 wheel ABI/hash/lazy-load/OCR positives; missing wheel, wrong arch, missing credential and timeout negatives | T15 |
| C16 | `@e-mate/dsh-plugin-xin-assistant` | T04 | Delete / P0 | No admitted UI: Desktop is `blocked`, CLI is retired -> dormant Tool/subprocess Host remains in source -> vendored Python/native runtime -> no public result projection | Inventory block and negative profile tests prove non-admission; source tree still contains executable/vendor closure | Delete snapshot in this document, three-target composition absence, packaged-runtime absence and migration cleanup of any old local profile; never alter historical desired-state bytes | T04 |

## Architecture ownership beyond the component rows

| ID | Area | Unique owner | Current source path and evidence | Required terminal evidence | Ticket |
|---|---|---|---|---|---|
| A01 | Session and Workspace | T02 | C01 uses pinned native client stores; Hosts use `WorkspaceRegistry`/native persistence rather than a parallel session store. Pinned Harness is `b2b1650b01f0ee88d81837a9b5c050f9f763f606`; upstream gitlinks are not initialized in this audit worktree. | Installed create/open/reconnect/rename/archive/restart and cross-workspace isolation, with no lost or duplicated execution | T02 |
| A02 | Tool registry and system prompt | T05 | Native rc.7 registry is the owner; components add typed Tools/prompts, and C14 restricts per Agent. Source tests cover late registration/child inheritance only. | Installed registry snapshot, prompt/header receipt, deterministic routing and denial paths | T05 |
| A03 | Schedule | T06 | Native schedule Tools own mutation and dispatch; C13 is a read-only projection. | Installed create/list/dispatch/delete/restart with exactly one scheduler | T06 |
| A04 | Skill lifecycle | T07 | Native Skill provider owns lifecycle; C02 owns enterprise publish/install and C07 is the fixed connector discovery exception. | Real two-account Skill Hub closure plus connector receipts | T07 |
| A05 | Job execution | T02 | Native Jobs are consumed by Skill Hub, Office, Image and QR paths; no component-local replacement was found. | Installed cancellation, restart/recovery, owner projection and no duplicate execution | T02 |
| A06 | Identity | T10 | C01 calls `/emate.identity`; profile identity Host uses enterprise auth/model gateways and OS credentials, then projects native identity state. | Registration/challenge/login/agreement/remember/logout/restart and policy-denial receipts | T10 |
| A07 | Online Share | T08 | C01 calls `/emate.share`; Host exports native Session ZIP through credentials to Share Worker `POST/GET/DELETE /v1/shares`, public `/s/<id>` projects the result. | Installed create/list/open/revoke/restart; revoked public URL returns the contracted terminal state | T08 |
| A08 | Profile generation | T01 | Signed desired state is staged/activated/rolled back through `profile-generation.ts`; current checked-in `profile-current-snapshot.json` is stale 2.0.12/v6 evidence. | Fresh three-target generated bytes, signed desired-state receipt and atomic rollback evidence | T01 |
| A09 | Desktop Update | T09 | Natural-language update Tool -> desktop update service -> Profile-first transaction -> Base fallback/updater -> native confirmation/progress/result. 2.0.14 repairs are source-only. | Installed 2.0.13 -> candidate confirmation, progress, restart, rollback and failure receipts | T09 |
| A10 | Enterprise Worker/service boundary | T18 | Auth gateway owns account actions; model gateway owns models/responses/images/audit/usage; analytics/admin owns policy/usage/status; Share and Skill Hub Workers remain purpose-specific. No local-runtime mutation route was admitted. | Deployed service/account receipts, authorization negatives and proof admin/model planes do not mutate the local Tool/runtime registry | T18 |

## T04 exact pre-delete snapshot

Snapshot was taken from the T03 baseline before any deletion:

- Delete the entire `packages/dsh-plugin-xin-assistant/` tree: **642 tracked files, 58,277,640 bytes**.
- That tree contains **497 `.py`/`.pyi` files**. The three `runtime/vendor-native` closures are: `darwin-arm64` 156 files / 22,382,672 bytes; `darwin-x64` 156 / 22,370,738 bytes; `win32-x64` 156 / 10,360,424 bytes.
- Delete its one row from `packages/dsh/profile/component-inventory.json` and its two byte-preservation rules from `.gitattributes`.
- Remove build/change-impact/component-release assumptions that require 16 rows or classify the xin package as a Base change. Convert release/profile checks to assert the 15-component future closure and package absence.
- Remove package-specific license/notice/environment/plugin-contract references that describe xin as current. Historical release/audit text may remain only in explicit past tense.
- No literal xin package entry was found in the current root `pnpm-lock.yaml`; T04 must still regenerate/check the lock rather than inventing an edit.
- Keep the xin identifier only where it is required as an explicit **retirement migration tombstone** in `packages/dsh/src/e-mate.ts` and `desktop/e-mate-desktop/src/e-mate-profile.ts`, and keep the matching stale-profile cleanup tests. It is not an active roster entry. T04 may remove that tombstone only after proving Profile reconciliation deletes an already-installed old xin directory and dependency without it.
- Public desired state already contains 15 components and never admits xin. T04 changes only source and the next generation; immutable historical objects are out of scope.

## T15 / T16 non-overlap contract

- T15 owns exactly `C04 CDP`, `C05 Computer Use`, `C07 Find Skill/connectors`, `C15 Vision Toolkit`.
- T16 owns exactly `C03 Better Sidebar`, `C06 File Import`, `C08 GenUI`, `C09 Glass Composer`, `C10 MCP Manage`, `C11 Memory Evolve`, `C12 Office Skills`.
- The remaining five inventory rows are excluded from both: C01 -> T13, C02 -> T07, C13 -> T06, C14 -> T05, C16 -> T04. T15 must not change Tool Search or Shell; T16 must not change Shell or native message/session ownership.

