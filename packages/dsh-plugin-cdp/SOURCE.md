# Source

This component is repository-owned TypeScript built on the Chrome DevTools Protocol exposed by the user's installed Chromium browser. It carries no browser binary, extension, Playwright runtime, MCP subprocess, or remotely downloaded executable.
