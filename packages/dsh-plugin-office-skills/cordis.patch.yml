# Register the package through the pinned Harness skill service. The skills
# are intentionally non-invocable while the exact execution-layer blocker is
# open; this bundle installs no Office, Python, OCR, or browser runtime.
- insert:
    - id: emate-office-skills
      name: '@e-mate/dsh-plugin-office-skills'
