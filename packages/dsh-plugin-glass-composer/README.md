# e-Mate Glass Composer

Adds a thin animated edge light to the existing DSH composer and a native settings button for selecting the palette. The setting is stored through the DSH settings namespace; the plugin does not replace the composer, inject HTML, or scan the page.
