# e-Mate DSH source package

`@e-mate/dsh@2.0.16` builds the product Profile and runtime source consumed by `desktop/e-mate-desktop`. It is not a second end-user installer, updater, or release channel. User delivery is owned exclusively by the native Desktop package.
